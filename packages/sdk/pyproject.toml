[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "flowcore-sdk"
version = "1.0.0"
description = "Official Python SDK for FlowCore integration."
authors = ["Rudran <rudran@example.com>"]
license = "MIT"
readme = "README.md"
packages = [{ include = "flowcore_sdk" }]

[tool.poetry.dependencies]
python = "^3.11"
httpx = "^0.27.0"
